# Monitoring - Uptime Kuma

Simple status monitoring with web UI.

## Features

- HTTP/HTTPS, TCP, Ping, DNS monitoring
- Status pages (public or private)
- Notification integrations (email, webhook, etc.)
- Certificate expiry monitoring

## Quick Start

```bash
just up monitoring
```

Access at `https://status.<BASE_DOMAIN>`.
