# CLAUDE.md — Claude agent instructions (deprecated)

This file previously duplicated the agent guidance found in `AGENTS.md`.
To avoid redundancy and keep a single source of truth, the canonical
instructions for all AI agents are maintained in `AGENTS.md` at the
repository root.

If you need Claude-specific notes or exceptions, please either:

- add a short `Agent-specific notes` section inside `AGENTS.md`, or
- create a dedicated file under `docs/agents/claude.md` and link it from
  `AGENTS.md`.

For now, update `AGENTS.md` for any changes that apply to Claude and the
other agents.

(End of file)
